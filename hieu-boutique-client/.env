# Frontend env for Hieu-Boutique (Vite)
# When VITE_API_URL is set, the client will call this base URL for product APIs.
VITE_API_URL=http://localhost:3000

# Base URL of backend server used by OAuth popup
VITE_SERVER_BASE=http://localhost:3000

# After creating/editing this file, restart Vite (npm run dev) to apply changes.
