// Chatbot action provider disabled — stub to avoid runtime errors if imported.
class ActionProvider {
  constructor() {}
  // no-op handlers
  handleMessage() {}
  handleHello() {}
  handleProducts() {}
  handleOrder() {}
  handleContact() {}
  handleDefault() {}
}

export default ActionProvider;